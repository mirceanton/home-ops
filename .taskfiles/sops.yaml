---
version: '3'

env:
  SOPS_AGE_KEY_FILE: /home/mike/.config/sops/age/keys.txt

tasks:
  encrypt:
    desc: Encrypt all sops files in this repository.
    run: once
    cmds: [ bash {{.ROOT_DIR}}/scripts/sops-encrypt-all.sh ]

  decrypt:
    desc: Decrypt all sops files in this repository.
    run: once
    silent: true
    cmds: [ bash {{.ROOT_DIR}}/scripts/sops-decrypt-all.sh ]
