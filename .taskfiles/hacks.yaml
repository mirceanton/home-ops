---
# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: '3'

tasks:
  delete-stuck-ns:
    desc: Delete all namespaces that are stuck in a terminating state.
    cmds:
      - bash scripts/delete-stuck-ns.sh
