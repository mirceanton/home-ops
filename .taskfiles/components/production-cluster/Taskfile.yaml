---
version: '3'

includes:
  talos: ./talos.yaml
  k8s: ./k8s.yaml
  # flux: ./flux.yaml

tasks:
  bootstrap:
    desc: Bootstrap the production cluster end to end.
    cmds:
      - task: talos:bootstrap
      - task: talos:wait
      - task: k8s:bootstrap
      - task: k8s:wait
      # - task: flux:bootstrap
      # - task: flux:wait
