---
version: '3'

tasks:
  bootstrap:
    desc: Install flux on the cluster.
    cmds:
      - kubectl create ns flux-system
      - cmd: |
          kubectl create secret generic sops-age \
            --namespace flux-systen \
            --from-file=age.agekey=/home/mike/.config/sops/age/keys.txt
