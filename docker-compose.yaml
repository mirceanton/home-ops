---
version: '3'

services:
  devbox:
    build: .
    volumes:
      - .:/workspace
      - /home/mike/.config/sops/age/keys.txt:/home/mike/.config/sops/age/keys.txt
      - /var/run/docker.sock:/var/run/docker.sock
    network_mode: "host"
