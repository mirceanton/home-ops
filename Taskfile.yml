---
version: '3'

env:
  SOPS_AGE_KEY_FILE: /home/mike/.config/sops/age/keys.txt

includes:
  tools: .taskfiles/tools.yaml
  sops: .taskfiles/sops.yaml
  docs: .taskfiles/docs.yaml

  mkc: .taskfiles/components/management-cluster
  akc: .taskfiles/components/application-cluster

tasks:
  default:
    silent: true
    cmds:
      - task -l
