---
version: '3'

env:
  SOPS_AGE_KEY_FILE: /home/mike/.config/sops/age/keys.txt

includes:
  hack: .taskfiles/hacks.yaml
  lint: .taskfiles/lint.yaml
  sops: .taskfiles/sops.yaml
  docs:
    taskfile: docs
    dir: docs

  home:
    taskfile: infra/home-cluster
    dir: infra/home-cluster
  infra:
    taskfile: infra/infra-cluster
    dir: infra/infra-cluster

tasks:
  default:
    silent: true
    cmds:
      - task -l
