---
version: '3'

env:
  SOPS_AGE_KEY_FILE: /home/mike/.config/sops/age/keys.txt

includes:
  hack: .taskfiles/hacks.yaml
  lint: .taskfiles/lint.yaml
  sops: .taskfiles/sops.yaml

  home:
    taskfile: clusters/home-cluster
    dir: clusters/home-cluster
  infra:
    taskfile: clusters/infra-cluster
    dir: clusters/infra-cluster

tasks:
  default:
    silent: true
    cmds:
      - task -l
