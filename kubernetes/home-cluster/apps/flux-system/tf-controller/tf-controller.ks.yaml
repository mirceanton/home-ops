---
# yaml-language-server: $schema=https://kubernetes-schemas.devbu.io/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cluster-apps-tf-controller
  namespace: flux-system
spec:
  path: ${CLUSTER_APPS_BASE_DIR}/flux-system/tf-controller
  targetNamespace: flux-system
  dependsOn:
    - name: cluster-apps-flux

  wait: true
  prune: true
  interval: ${KUSTOMIZATION_INTERVAL}
  retryInterval: ${KUSTOMIZATION_RETRY_INTERVAL}
  timeout: ${KUSTOMIZATION_TIMEOUT}
  sourceRef:
    kind: GitRepository
    name: home-ops-git-repo