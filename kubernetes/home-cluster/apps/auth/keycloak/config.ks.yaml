---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cluster-apps-keycloak-config
  namespace: flux-system
spec:
  path: ${CLUSTER_APPS_BASE_DIR}/auth/keycloak/config
  targetNamespace: auth
  dependsOn:
    - name: cluster-apps-tf-controller
    - name: cluster-apps-keycloak
