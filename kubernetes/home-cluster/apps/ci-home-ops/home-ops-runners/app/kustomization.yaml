---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - home-ops-runners.helm-rekease.yaml
  - runner-scale-set-controller.helm-repository.yaml

configMapGenerator:
  - name: home-ops-runners-values
    options:
      disableNameSuffixHash: true
    files:
      - values.yaml=home-ops-runners.helm-values.yaml
