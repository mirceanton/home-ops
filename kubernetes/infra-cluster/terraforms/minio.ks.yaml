---
# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/kustomization-kustomize-v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cluster-terraforms-minio
  namespace: flux-system
  labels:
    patching.home.mirceanton.com/source-ref: true
    patching.home.mirceanton.com/intervals: true
    patching.home.mirceanton.com/post-build: false
    patching.home.mirceanton.com/decryption: true
spec:
  path: kubernetes/infra-cluster/apps/terraform-system/terraforms/minio
  targetNamespace: terraform-system
  dependsOn:
    - name: cluster-apps-tf-controller
    - name: cluster-apps-minio
