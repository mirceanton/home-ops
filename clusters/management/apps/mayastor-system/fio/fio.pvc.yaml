---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mayastor-test-claim
  namespace: mayastor-system
spec:
  storageClassName: mayastor
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
