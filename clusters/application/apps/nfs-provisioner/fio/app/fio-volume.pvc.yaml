---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: nfs-test-claim
  namespace: nfs-provisioner-system
spec:
  storageClassName: truenas-nfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
