---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: sonarr-secret
spec:
  refreshInterval: 1h

  secretStoreRef:
    name: onepassword
    kind: ClusterSecretStore

  target:
    name: sonarr-secret
    creationPolicy: Owner

  data:
    - secretKey: api_key
      remoteRef:
        key: Sonarr API Key
        property: password
