---

apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: onepassword
spec:
  provider:
    onepassword:
      connectHost: http://onepassword.security-system.svc.cluster.local
      vaults:
        Automation: 1
      auth:
        secretRef:
          connectTokenSecretRef:
            name: op-token
            namespace: security-system
            key: token
