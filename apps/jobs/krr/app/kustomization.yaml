---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./external-secret.yaml
  - ./oci-repository.yaml
  - ./helm-release.yaml

configMapGenerator:
  - name: krr-script
    files:
      - ./files/open-issue.py

generatorOptions:
  disableNameSuffixHash: true
