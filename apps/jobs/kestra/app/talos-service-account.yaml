---
# Talos API credentials for flows that drive talosctl (etcd defrag, ...). Talos materializes a
# Secret of the same name (key `config`, a talosconfig) that PodCreate job pods mount.
apiVersion: talos.dev/v1alpha1
kind: ServiceAccount
metadata:
  name: kestra-talos
spec:
  roles: ["os:admin"]
