---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: descheduler
spec:
  interval: 10m
  prune: true
  wait: true
  targetNamespace: kube-system

  path: ./apps/kube-system/descheduler/app
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system

  postBuild:
    substitute: {}

  dependsOn: []
