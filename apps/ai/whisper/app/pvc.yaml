---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: whisper-cache
spec:
  storageClassName: openebs-zfs-cache
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
