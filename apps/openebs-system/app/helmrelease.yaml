---

apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openebs
spec:
  interval: 10m
  chartRef:
    kind: OCIRepository
    name: openebs

  values:
    localpv-provisioner:
      localpv:
        basePath: &hostPath /var/mnt/data
        enableLeaderElection: true #! Required for more than 1 replica
        replicas: 1 #! TODO: Set to 2 or more when adding more nodes

        # ===================== Resource Allocations =====================
        requests:
          cpu: 20m
          memory: 32Mi
        limits:
          cpu: 50m
          memory: 128Mi

      # ========================== StorageClass ==========================
      hostpathClass:
        enabled: true
        name: openebs-hostpath
        reclaimPolicy: Delete
        isDefaultClass: true
        basePath: *hostPath

    # ====================================================================
    # ====================== Disable EVERYTHING Else =====================
    # ====================================================================
    analytics: {enabled: false}
    alloy: {enabled: false}
    loki: {enabled: false}
    engines:
      local:
        lvm: {enabled: false}
        zfs: {enabled: false}
      replicated:
        mayastor: {enabled: false}
    openebs-crds:
      csi:
        volumeSnapshots:
          enabled: false
          keep: false
