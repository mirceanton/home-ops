---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: github-runners-cache
spec:
  storageClassName: openebs-zfs
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 20Gi
