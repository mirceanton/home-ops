---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app karakeep
spec:
  interval: 10m
  prune: true
  wait: true
  timeout: 15m
  targetNamespace: media

  path: ./apps/media/karakeep/app
  components:
    - ../../../../components/volsync/
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system

  postBuild:
    substitute:
      APP: *app
      VOLSYNC_CAPACITY: 16Mi
      VOLSYNC_PUID: "568"
      VOLSYNC_PGID: "568"

  decryption: {provider: sops}
  dependsOn:
    - name: 1password-connect
      namespace: security-system
    - name: volsync
      namespace: storage-system
