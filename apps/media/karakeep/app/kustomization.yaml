---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./karakeep.helm-release.yaml
  - ./karakeep.oci-repository.yaml
  - ./external-secret.yaml
  - ./grafana-dashboard.yaml

configMapGenerator:
  - name: karakeep-dashboard
    options:
      disableNameSuffixHash: true
    files:
      - dashboard.json
