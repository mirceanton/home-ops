---
# yaml-language-server: $schema=https://k8s-schemas.mirceanton.com/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: zfs-exporter
spec:
  interval: 10m
  chartRef:
    kind: OCIRepository
    name: zfs-exporter

  values:
    global:
      createDefaultServiceAccount: false

    controllers:
      zfs-exporter:
        type: statefulset
        annotations:
          reloader.stakater.com/auto: "true"

        containers:
          zfs-exporter:
            image:
              repository: ghcr.io/mirceanton/zfs-exporter
              tag: 2.3.12@sha256:7eb88fff982c239b9b3c52429e771b859e3c944ca41808be0fd6194eab3f1e6f
            resources:
              requests:
                cpu: 10m
                memory: 32Mi
              limits:
                memory: 64Mi

            #? Needs privileged access to /dev/zfs to query pool/dataset state via libzfs ioctls.
            #? On Talos, ZFS is loaded as a kernel extension so /dev/zfs is available on the host.
            securityContext:
              privileged: true

    defaultPodOptions:
      automountServiceAccountToken: false
      securityContext:
        runAsUser: 0
        runAsGroup: 0

    service:
      zfs-exporter:
        ports:
          http:
            port: 9134

    serviceMonitor:
      zfs-exporter:
        serviceName: zfs-exporter
        endpoints:
          - port: http
            scheme: http
            path: /metrics
            interval: 1m
            scrapeTimeout: 30s

    persistence:
      dev-zfs:
        type: hostPath
        hostPath: /dev/zfs
        globalMounts:
          - path: /dev/zfs
