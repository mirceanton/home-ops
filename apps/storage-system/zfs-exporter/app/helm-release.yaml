---
# yaml-language-server: $schema=https://k8s-schemas.mirceanton.com/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: zfs-exporter
spec:
  interval: 10m
  chartRef:
    kind: OCIRepository
    name: zfs-exporter

  values:
    global:
      createDefaultServiceAccount: false

    controllers:
      zfs-exporter:
        type: statefulset
        annotations:
          reloader.stakater.com/auto: "true"

        containers:
          zfs-exporter:
            image:
              repository: ghcr.io/mirceanton/zfs-exporter
              tag: 2.4.0@sha256:bf42f9815061bb0acdcd23cfb8aa6e49ff3b8e313a5850534d04f5e3851d1504
            resources:
              requests:
                cpu: 10m
                memory: 32Mi
              limits:
                memory: 64Mi

            #? Needs privileged access to /dev/zfs to query pool/dataset state via libzfs ioctls.
            #? On Talos, ZFS is loaded as a kernel extension so /dev/zfs is available on the host.
            securityContext:
              privileged: true

    defaultPodOptions:
      automountServiceAccountToken: false
      securityContext:
        runAsUser: 0
        runAsGroup: 0

    service:
      zfs-exporter:
        ports:
          http:
            port: 9134

    serviceMonitor:
      zfs-exporter:
        serviceName: zfs-exporter
        endpoints:
          - port: http
            scheme: http
            path: /metrics
            interval: 1m
            scrapeTimeout: 30s

    persistence:
      dev-zfs:
        type: hostPath
        hostPath: /dev/zfs
        globalMounts:
          - path: /dev/zfs
