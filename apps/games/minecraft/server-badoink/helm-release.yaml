---
# yaml-language-server: $schema=https://k8s-schemas.mirceanton.com/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app minecraft-server-badoink
spec:
  interval: 15m
  chartRef:
    kind: OCIRepository
    name: minecraft

  values:
    fullnameOverride: *app
    workloadAsStatefulSet: true
    strategyType: OnDelete

    image:
      repository: ghcr.io/itzg/minecraft-server
      tag: 2026.7.0@sha256:5ffbc91d933e2f0e194a4f49674a2d17b65bb051c86e61961a6c775312ef61c0

    resources:
      requests:
        cpu: 200m
        memory: 1Gi
      limits:
        memory: 4Gi

    podSecurityContext:
      runAsUser: 1000
      runAsGroup: 3000
      runAsNonRoot: true
      fsGroup: 2000
      seccompProfile:
        type: RuntimeDefault

    securityContext:
      readOnlyRootFilesystem: true
      allowPrivilegeEscalation: false
      runAsUser: 1000
      fsGroup: 100
      capabilities:
        drop: ["ALL"]

    livenessProbe:
      initialDelaySeconds: 30
    readinessProbe:
      initialDelaySeconds: 30
    startupProbe:
      enabled: true

    extraEnv:
      TZ: Europe/Bucharest

    persistence:
      dataDir:
        enabled: true
        existingClaim: ${APP}

    serviceAnnotations:
      mc-router.itzg.me/externalServerName: badoink-mc.home.mirceanton.com

    minecraftServer:
      eula: true
      # renovate: datasource=custom.minecraft-release
      version: 1.21.11
      type: PAPER
      overrideServerProperties: true
      jvmXXOpts: "-XX:MaxRAMPercentage=75"
      difficulty: normal #? peaceful, easy, normal, hard
      spawnProtection: 0
      gameMode: survival #? creative, survival, adventure, spectator
      pvp: false
      onlineMode: true
      ops: "MrBorboto,bomkii" #? comma separated list of operators
      motd: "Badoink Minecraft Server"
      worldSaveName: "Badoink"
      viewDistance: 16

      spigetResources:
        - 36618 #Prom Exporter
      rcon:
        enabled: true
        existingSecret: *app
        secretKey: RCON_PASSWORD
