---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./oci-repository.yaml
  - ./helm-release.yaml
  - ./external-secret.yaml
  - ./local.pvc.yaml

configMapGenerator:
  - name: selkies-sunshine-config
    options:
      disableNameSuffixHash: true
      annotations:
        kustomize.toolkit.fluxcd.io/substitute: disabled
    files:
      - files/sunshine.conf
      - files/apps.json
